export type FilmModel = {
    title: string,
    actors: string,
    runtime: string;
    released: string,
    year: string,
    rated: string,
    imdbRating: string,
    genre: string,
    poster: string,
    imdbID: string,
    plot: string,
    response: string,
    favorite?: boolean,
}